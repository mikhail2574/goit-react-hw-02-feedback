import React, { Fragment, Component } from "react";
import { ReactDOM } from "react-dom";
import { Form } from "./Form/Form";

export const App = () => {
  return (
    <Fragment>
      <Form />
    </Fragment>
  );
};
